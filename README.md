Adds input method for Cyrillic Apple Keyboard with Numeric Keypad to
use it in Emacs.

To enable add something like this into your .emacs file:
`(load-file "~/.emacs.d/apple-cyrillic.el")`
`(set-input-method "apple-cyrillic")`
